
# Global tags can be specified here in key="value" format.
[global_tags]
  # dc = "us-east-1" # will tag all metrics with dc=us-east-1
  # rack = "1a"
  ## Environment variables can be used as tags, and throughout the config file
  # user = "$USER"


# Configuration for telegraf agent
[agent]
  ## Default data collection interval for all inputs
  interval = "5s"
  ## Rounds collection interval to 'interval'
  ## ie, if interval="10s" then always collect on :00, :10, :20, etc.
  round_interval = true

  ## Telegraf will send metrics to outputs in batches of at most
  ## metric_batch_size metrics.
  ## This controls the size of writes that Telegraf sends to output plugins.
  metric_batch_size = 1000

  ## Maximum number of unwritten metrics per output.  Increasing this value
  ## allows for longer periods of output downtime without dropping metrics at the
  ## cost of higher maximum memory usage.
  metric_buffer_limit = 100000

  ## Collection jitter is used to jitter the collection by a random amount.
  ## Each plugin will sleep for a random time within jitter before collecting.
  ## This can be used to avoid many plugins querying things like sysfs at the
  ## same time, which can have a measurable effect on the system.
  collection_jitter = "0s"

  ## Collection offset is used to shift the collection by the given amount.
  ## This can be be used to avoid many plugins querying constraint devices
  ## at the same time by manually scheduling them in time.
  # collection_offset = "0s"

  ## Default flushing interval for all outputs. Maximum flush_interval will be
  ## flush_interval + flush_jitter
  flush_interval = "1s"
  ## Jitter the flush interval by a random amount. This is primarily to avoid
  ## large write spikes for users running a large number of telegraf instances.
  ## ie, a jitter of 5s and interval 10s means flushes will happen every 10-15s
  flush_jitter = "0s"

  ## Collected metrics are rounded to the precision specified. Precision is
  ## specified as an interval with an integer + unit (e.g. 0s, 10ms, 2us, 4s).
  ## Valid time units are "ns", "us" (or "µs"), "ms", "s".
  ##
  ## By default or when set to "0s", precision will be set to the same
  ## timestamp order as the collection interval, with the maximum being 1s:
  ##   ie, when interval = "10s", precision will be "1s"
  ##       when interval = "250ms", precision will be "1ms"
  ##
  ## Precision will NOT be used for service inputs. It is up to each individual
  ## service input to set the timestamp at the appropriate precision.
  precision = ""

  ## Override default hostname, if empty use os.Hostname()
  hostname = ""
  ## If set to true, do no set the "host" tag in the telegraf agent.
  omit_hostname = false
  #debug = true
  #logfile = "/tmp/telegraf.log"

###############################################################################
#                            OUTPUT PLUGINS                                   #
###############################################################################

# # Configuration for sending metrics to InfluxDB
[[outputs.influxdb_v2]]
  ## The URLs of the InfluxDB cluster nodes.
  ##
  ## Multiple URLs can be specified for a single cluster, only ONE of the
  ## urls will be written to each interval.
  ##   ex: urls = ["https://us-west-2-1.aws.cloud2.influxdata.com"]
  urls = ["http://${DOCKER_INFLUXDB_INIT_HOST}:8086"]
  #urls = ["https://us-east-1-1.aws.cloud2.influxdata.com/"]

  ## Token for authentication.
  token = "$DOCKER_INFLUXDB_INIT_ADMIN_TOKEN"
  #token = "QKK1qmWnJ8wvMqCHTYwqSt5hvZd78MklCBqYqFvW_dy9g7kV6DnwnpKI0x--YY2Gr3r9UeSN4B_bE-vlaDRUWQ=="
  #token = "G89wzEYgIfkBfLDPdNtnz7wVLRrBWU8HpIeBhcQ2owTI95utQodP0Xzq8gSqI62WxD86w2ZBlY8lzsZlHwtKXA=="

  ## Organization is the name of the organization you wish to write to; must exist.
  organization = "$DOCKER_INFLUXDB_INIT_ORG"

  ## Destination bucket to write into.
  bucket = "$DOCKER_INFLUXDB_INIT_BUCKET"
  #bucket = "FlexUNS_Bucket"

  content_encoding = "gzip" # for better performance

# Telegraf Configuration for MQTT - Storage
[[inputs.mqtt_consumer]]
  #servers = ["tcp://broker.hivemq.com:1883"]  # HiveMQ broker
  #servers = ["ssl://zb191690.ala.eu-central-1.emqxsl.com:8883"]  # EMQX Personal broker
  #servers = ["ssl://d6343f2567d641e4a0e22d56e9492a04.s1.eu.hivemq.cloud:8883"]  # HiveMQ Personal broker
  servers = ["ssl://5dcd5cd5264848d1a3c069eb39cf819e.s1.eu.hivemq.cloud:8883"]  # HiveMQ Personal broker
  username = "explorer"  # MQTT username
  password = "joacoL21"  # MQTT password
  #servers = ["ssl://oa19fdef.ala.dedicated.aws.emqxcloud.com:8883"]  # EMQX Dautom broker
  #username = "gilbert.delgado"  # MQTT username
  #password = "password"  # MQTT password
  client_id = "telegraf_storage"  # Client ID for MQTT connection
  #topics = ["Enterprise/Site/Area/Motor1/Edge/MotorModel/vibration", "Enterprise/Site/Area/Motor2/Edge/MotorModel/vibration"]  # MQTT topic for vibration data
  topics = ["Enterprise/Site/Area/+/Edge/MotorModel/vibration"]  # MQTT topic for vibration data
  data_format = "json"  # Expecting JSON payload
  json_time_key = "timestamp"  # Use "timestamp" from JSON data
  json_time_format = "unix_ns"  # Timestamp is in nanoseconds
  json_query = "sensor_data"  # Extract array of sensor readings
  tag_keys = ["topic", "batch_id"]  # Prevents overwriting by adding batch_id as a tag
  name_override = "vibration_data"  # Rename measurement in InfluxDB

# Telegraf Configuration for MQTT - FFT
[[inputs.mqtt_consumer]]
  #servers = ["tcp://broker.hivemq.com:1883"]  # HiveMQ broker
  #servers = ["ssl://zb191690.ala.eu-central-1.emqxsl.com:8883"]  # EMQX Personal broker
  #servers = ["ssl://d6343f2567d641e4a0e22d56e9492a04.s1.eu.hivemq.cloud:8883"]  # HiveMQ Personal broker
  servers = ["ssl://5dcd5cd5264848d1a3c069eb39cf819e.s1.eu.hivemq.cloud:8883"]  # HiveMQ Personal broker
  username = "explorer"  # MQTT username
  password = "joacoL21"  # MQTT password
  #servers = ["ssl://oa19fdef.ala.dedicated.aws.emqxcloud.com:8883"]  # EMQX Dautom broker
  #username = "gilbert.delgado"  # MQTT username
  #password = "password"  # MQTT password
  client_id = "telegraf_fft"  # Client ID for MQTT connection
  #topics = ["Enterprise/Site/Area/Motor1/Analysis/Vibration/fft", "Enterprise/Site/Area/Motor2/Analysis/Vibration/fft"]  # MQTT topic for vibration data
  topics = ["Enterprise/Site/Area/+/Analysis/Vibration/fft"]  # MQTT topic for vibration data
  data_format = "json"  # Expecting JSON payload
  json_time_key = "timestamp"  # Use "timestamp" from JSON data
  json_time_format = "unix_ns"  # Timestamp is in nanoseconds
  json_query = "fft_data"  # Extract array of sensor readings
  tag_keys = ["topic", "batch_id"]  # Prevents overwriting by adding batch_id as a tag
  name_override = "fft_vibration"  # Rename measurement in InfluxDB

# Telegraf Configuration for MQTT - Fault Predictions
[[inputs.mqtt_consumer]]
  #servers = ["tcp://broker.hivemq.com:1883"]  # HiveMQ broker
  #servers = ["ssl://zb191690.ala.eu-central-1.emqxsl.com:8883"]  # EMQX Personal broker
  #servers = ["ssl://d6343f2567d641e4a0e22d56e9492a04.s1.eu.hivemq.cloud:8883"]  # HiveMQ Personal broker
  servers = ["ssl://5dcd5cd5264848d1a3c069eb39cf819e.s1.eu.hivemq.cloud:8883"]  # HiveMQ Personal broker
  username = "explorer"  # MQTT username
  password = "joacoL21"  # MQTT password
  #servers = ["ssl://oa19fdef.ala.dedicated.aws.emqxcloud.com:8883"]  # EMQX Dautom broker
  #username = "gilbert.delgado"  # MQTT username
  #password = "password"  # MQTT password
  client_id = "telegraf_diagnosis"  # Client ID for MQTT connection
  #topics = ["Enterprise/Site/Area/Motor1/Analysis/Diagnosis/prediction", "Enterprise/Site/Area/Motor2/Analysis/Diagnosis/prediction"]  # Topic for CNN fault predictions
  topics = ["Enterprise/Site/Area/+/Analysis/Diagnosis/prediction"]  # Topic for CNN fault predictions
  data_format = "json"  # Expecting JSON payload

  # Time Configuration
  json_time_key = "timestamp"  # Use timestamp field
  json_time_format = "unix_ns"  # Timestamp in nanoseconds

  # Custom measurement name
  name_override = "fault_diagnosis"  # This renames the measurement in InfluxDB

  # Parse the full JSON message
  json_query = ""  

  # Store metadata as tags
  tag_keys = ["topic"]  

  #[[processors.starlark]]
  #script = "/mnt/w/Git/major-cbs-framework/InfluxData/tig-stack/telegraf/downsample.star" # Adjust path if needed

